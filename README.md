# Network diagrams

To use, place in a directory with a file called config.json with the same format as the one in the repo.

Each task must have ID, description, duration, and a list of predecessors.
The list of predecessors defines what tasks must be done before work on this task can be started.
The predecessor list is a list of task-IDs
